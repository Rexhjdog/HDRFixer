<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HDRFixer.App.Views.FixesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:HDRFixer.App.ViewModels">
    <ScrollViewer Padding="24">
        <StackPanel Spacing="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{x:Bind ViewModel.Title}" Style="{StaticResource TitleTextBlockStyle}" />
                    <TextBlock Text="Apply and manage HDR correction fixes" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7" />
                </StackPanel>
                <Button Grid.Column="1" Content="Revert All" Command="{x:Bind ViewModel.RevertAllCommand}" VerticalAlignment="Bottom" />
            </Grid>

            <ItemsControl ItemsSource="{x:Bind ViewModel.Fixes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:FixViewModel">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,16">
                            <StackPanel Spacing="8">
                                <TextBlock Text="{x:Bind Name}" Style="{StaticResource SubtitleTextBlockStyle}" />
                                <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" Opacity="0.7" />
                                <TextBlock Text="{x:Bind StatusMessage, Mode=OneWay}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button Content="Apply" Command="{x:Bind ApplyCommand}" Style="{StaticResource AccentButtonStyle}" />
                                    <Button Content="Revert" Command="{x:Bind RevertCommand}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>
